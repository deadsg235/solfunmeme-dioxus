[package]
name = "clifford_flow_provider"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Path to the bootstrap crate for Clifford algebra implementation
bootstrap = { path = "../../bootstrap" }
